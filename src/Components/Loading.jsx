import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen flex items-center justify-center'>
            <title>Clean Sphere | Loading</title>
            <span className="loading loading-ring loading-xl"></span>
        </div>
    );
};

export default Loading;